# Test Matrix

Unit tests
- Config loading and validation
- Split logic (group leakage guard)
- Imputation hierarchy selection
- Feature builders (core, artist, genre, temporal)
- Feature block registry and pipeline composition

Integration tests
- End-to-end dataset preparation on fixture CSV
- Bayesian model fit on synthetic data

E2E tests
- Full pipeline run on small sample
