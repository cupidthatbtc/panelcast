coverage:
  status:
    project:
      default:
        target: 95%
        threshold: 1%
    patch:
      default:
        target: 90%
        threshold: 1%
        # Blocking. The opt-in model-v2 paths (EIV / RW-horizon propagation) that
        # forced the advisory downgrade (PR #32) now have fast-suite tests PR CI
        # runs — e.g. test_fit_eiv.py's gate-logic tests and test_rw_horizon.py —
        # so diff coverage no longer understates them. Project (95%) stays a gate.
